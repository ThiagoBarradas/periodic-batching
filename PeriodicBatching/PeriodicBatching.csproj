<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Authors>Thiago Barradas</Authors>
    <Company>Thiago Barradas</Company>
    <Copyright>Ⓒ Thiago Barradas</Copyright>
    <PackageLicenseUrl>https://github.com/ThiagoBarradas/periodic-batching/blob/master/LICENSE</PackageLicenseUrl>
    <PackageProjectUrl>https://github.com/ThiagoBarradas/periodic-batching</PackageProjectUrl>
    <RepositoryUrl>https://github.com/ThiagoBarradas/periodic-batching</RepositoryUrl>
    <PackageTags>periodic batching batch period timely scheduler events event processor queue rabbit serilog buffer cron crontab</PackageTags>
    <PackageIconUrl>https://i.imgur.com/Yo7pa2d.png</PackageIconUrl>
    <Description>Execute a batching function with a controlled size every some specified time interval. Very useful for background processes to send events to some broker or something like it.</Description>
  </PropertyGroup>

</Project>
